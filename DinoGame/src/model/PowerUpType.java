package model;

/**
 * Enum for power-up types. Used by PowerUpFactory.
 */
public enum PowerUpType {
    SHIELD,      // Absorbs one collision
    SLOW_MOTION, // Reduces game speed temporarily
    SCORE_X2     // Doubles score for duration
}
